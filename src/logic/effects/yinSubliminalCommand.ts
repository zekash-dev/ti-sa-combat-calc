import { ParticipantTagImplementation } from "model/effects";

export const yinSubliminalCommand: ParticipantTagImplementation = {
    // No explicit implementation required. The implementation of "Indoctrinate" takes this tag into account.
};
